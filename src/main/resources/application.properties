spring.application.name=ms_tracking
server.port=8093

spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/db_ms_tracking?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format.sql=true
logging.level.org.hibernate.orm.jdbc.bind=${HIBERNATE_BIND_LOG:OFF}

eureka.client.service-url.defaultZone=${EUREKA_URI:http://localhost:8761/eureka/}

springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html

ms.admision.url=${MS_ADMISION_URL:http://localhost:8087}
